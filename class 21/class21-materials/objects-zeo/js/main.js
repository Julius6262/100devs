//Create a stopwatch object that has four properties and three methods

//literal notation
let stopwatch = {}

stopwatch.color = "black"
stopwatch.height = 40
stopwatch.width = 50
stopwatch.light = false

const stopwatch.start = ((light)) =>({
    stopwatch.light = true
})

const stopwatch.start = () =>(console.log("time started))

stopwatch.takeTime()
stopwatch.start()

